{% extends 'base.html.twig' %}

{% block title %}Patient{% endblock %}

{% block body %}
    <h1>Patient</h1>

<h2>Modification patient</h2>
    {{ include('patient/_form.html.twig', {form:formEdition} ) }}

    <a href="{{ path('patient_index') }}">back to list</a>
    

    <a href="{{ path('patient_edit', {'id': patient.id}) }}">edit</a>

    <h2>Création d'une admision</h2>
    {{ include('patient/_form.html.twig', {form:createAdmission} ) }}

    {{ include('patient/_delete_form.html.twig') }}
   
  
{% endblock %}
